spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://photospotmap.crxyn5dktsnd.ap-northeast-2.rds.amazonaws.com:3306/photo_spot_map?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: photospotmap1234!

  jpa:
    database: mysql
    open-in-view: true
    hibernate:
      ddl-auto: create #운영장비에서는 절대 사용해서는 안됨
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

logging:
  level:
    org.hibernate.SQL: debug